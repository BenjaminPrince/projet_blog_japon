{% set menus = menu_service.findAll %}


<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>Blog - {% block title %}{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
        {# <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous"> #}
        <link href={{asset('build/style.css')}} rel="stylesheet">
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
        {# <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script> #}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>

    <div class="image-nav">
        <a href="#">      	   
        <img  class="imageNav" src="https://cdn.pixabay.com/photo/2017/05/11/23/21/mount-fuji-2305606_960_720.jpg" width="2435" height="659" alt="JAPON" ></a>			
    </div>


     <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container d-flex flex-wrap">
                <div class="dropdown d-flex">
                    <a class="navbar-brand nav nav-link dropdown-toggle text-white" role="button" id="dropdownAdminLink" data-bs-toggle="dropdown" aria-expanded="false">
                        Blog Japon
                    </a> 
                    {% if is_granted('ROLE_AUTHOR') %}
                    <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdminDashboard">
                    <li><a class="dropdown-item" href="{{path('admin')}}">Tableau de bord</a></li>
                        {# <li><a class="dropdown-item" href="{{ ea_index('MenuCrudController') }}">Menus</a></li> #}
                        {% endif %}
                    </ul>
                        <a class="link-nav-header text-white p-8" href="{{path('app_home')}}">Articles Récents </a>
                        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                            <a class="link-nav-header text-white" href="{{ path('app_register') }}">S'inscrire</a>
                            <a class="link-nav-header text-white" href="{{ path('app_login') }}">Se connecter</a>
                        {% else %}
                            <p class="text-white link-nav-header">Pseudo: {{ app.user.username|capitalize }}</p>
                            <a class="link-nav-header text-white" href="{{ path('app_profile',{ username: app.user.username }) }}">Mon profil</a>
                            <a class="link-nav-header text-white" href="{{ path('app_logout') }}">Deconnexion</a> 
                        {% endif %}
                </div>
            </div>
     </nav>
{# 
                <div class="dropdown">
                <a class="nav nav-link dropdown-toggle text-white" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                     Ajouter
                </a>
                <ul class="dropdown-menu" aria-labelledby="dropdownMenuButtonAdminAdd">
                    <li><a class="dropdown-item" href="#">Article</a></li>
                    <li><a class="dropdown-item" href="#">Page</a></li>
                </ul>
                </div> #}

    {# {% endif %} #}

    {# <div class="container">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        
            <a class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none" href="{{path('app_home')}}"
                    <span class="fs-4">Blog Japon</span>
            </a>

        <ul class="nav nav-pills"> #}

            {# {% for menu in menus %}
                        {% if menu.subMenus.count > 0 %}
                            <div class="dropdown">
                                <a class="nav nav-link text-dark dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                {{ menu.name }}
                                </a>
                               <ul class="dropdown-menu">
                                    {% for subMenu in menu.subMenus %}
                                        <li>
                                        <a class="dropdown-item" href="{{ subMenu|menuLink }}"> {{ subMenu.name }} </a>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </div>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link text-dark" href="#"> {{ menu.name }}</a>
                            </li>
                        {% endif %}
                    {% endfor %} #}

            {# {% if not is_granted('IS_AUTHENTICATED_FULLY') %} #}
           
            {# <li class="nav-item">
                <a class="nav-link text-dark"  href="{{ path('app_register') }}">S'inscrire</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark"  href="{{ path('app_login') }}">Se connecter</a>
            </li>   #}
          
            {# {% else %} #}
                {# <div class="dropdown">
                    <a class="nav nav-link text-dark dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    {{ app.user.username|capitalize }}
                    </a>
                    <ul class="dropdown-item">
                        <li>
                            <a class="dropdown-item" href="{{ path('app_profile',{ username: app.user.username }) }}"> Mon profil</a>
                        </li>
                         <li>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                    </ul>
                </div> #}
            {# {% endif %}   #}
        {# </ul>
          
        </header>
    </div> #}

        {% block body %}{% endblock %}

            
    <footer>
           
                <span class="copyright">© Benjamin Prince {{ 'now'|date('Y') }}</span>

    </footer>
           
    </body> 
</html>
